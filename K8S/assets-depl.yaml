apiVersion: apps/v1
kind: Deployment
metadata:
  name: assets-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: assets-depl
  template:
    metadata:
      labels:
        app: assets-depl
    spec:
      containers:
        - name: assets-depl
          image: assetstoreregistry.azurecr.io/assetservice:v0.1
          ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: assets-depl
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: assets-depl