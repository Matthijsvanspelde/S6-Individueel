apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: userservice
  template:
    metadata:
      labels:
        app: userservice
    spec:
      containers:
        - name: userservice
          image: assetstoreregistry.azurecr.io/userservice:v0.1
          ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: users-depl
spec:
  selector:
    app: userservice
  ports:
    - port: 8765
      targetPort: 9376
  type: LoadBalancer